import { NgForDirective } from './ng-for.directive';

describe('NgForDirective', () => {
  it('should create an instance', () => {
    const directive = new NgForDirective();
    expect(directive).toBeTruthy();
  });
});
